# -simple_rule-based_chatbot
It's a command-line application with hardcoded responses that:  Matches exact user inputs (like "hello", "how are you", "what is your name") against predefined patterns Returns pre-programmed responses for each matched input Falls back to "Sorry, I don't understand" for unrecognized inputs Exits when the user types "bye"
